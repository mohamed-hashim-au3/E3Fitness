import { connect } from "react-redux";
import YouthClassComponent from "../components/YouthClass";

const mapStateToProps = (state, ownProps) => {
  return {};
};

const mapDispatchToProps = (dispatch, ownProps) => {
  return {};
};

const YouthClass = connect(
  mapStateToProps,
  mapDispatchToProps
)(YouthClassComponent);

export default YouthClass;
