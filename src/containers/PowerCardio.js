import { connect } from "react-redux";
import PowerCardioComponent from "../components/PowerCardio";

const mapStateToProps = (state, ownProps) => {
  return {};
};

const mapDispatchToProps = (dispatch, ownProps) => {
  return {};
};

const PowerCardio = connect(
  mapStateToProps,
  mapDispatchToProps
)(PowerCardioComponent);

export default PowerCardio;
