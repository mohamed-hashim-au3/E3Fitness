import { connect } from "react-redux";
import PowerDanceComponent from "../components/PowerDance";

const mapStateToProps = (state, ownProps) => {
  return {};
};

const mapDispatchToProps = (dispatch, ownProps) => {
  return {};
};

const PowerDance = connect(
  mapStateToProps,
  mapDispatchToProps
)(PowerDanceComponent);

export default PowerDance;
