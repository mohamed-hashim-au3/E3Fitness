import { connect } from "react-redux";
import PowerZenComponent from "../components/PowerZen";

const mapStateToProps = (state, ownProps) => {
  return {};
};

const mapDispatchToProps = (dispatch, ownProps) => {
  return {};
};

const PowerZen = connect(
  mapStateToProps,
  mapDispatchToProps
)(PowerZenComponent);

export default PowerZen;
