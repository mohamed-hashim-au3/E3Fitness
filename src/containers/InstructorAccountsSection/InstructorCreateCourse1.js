import { connect } from "react-redux";
import InstructorCreateCourse1 from "../../components/InstructorAccountSection/InstructorCreateCourse1";

const mapStateToProps = (state, ownProps) => {
  return {};
};

const mapDispatchToProps = (dispatch, ownProps) => {
  return {};
};

const CreateCourse1 = connect(
  mapStateToProps,
  mapDispatchToProps
)(InstructorCreateCourse1);

export default CreateCourse1;
